using UnityEngine;

public class Pickupable : MonoBehaviour
{
    // This script is intentionally left empty.
    // It's just a marker to indicate that this GameObject can be picked up (besides using tag system).
}
